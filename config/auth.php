<?php

// This is the model used for authentication in Doppar.
// By default, it uses App\Models\User, but the user can change this
// to point to a different model if custom authentication logic is needed.
return [
    'model' => App\Models\User::class
];
